# Web 服务
fastapi>=0.115.0
uvicorn>=0.30.0
gradio>=4.31.0

# LangChain 生态 (新版)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.1.20
langgraph>=0.2.0
langchain-chroma>=0.1.0  # 新增：rag_service.py 使用 Chroma 类

# 数据库
neo4j>=5.24.0
chromadb>=0.5.4
redis>=5.0.0  # 新增：session_store.py 使用 Redis 会话存储

# 配置与模型
pydantic-settings>=2.0.0
python-dotenv>=1.0.1

# 工具与日志
tenacity>=8.3.0
structlog>=23.0.0
tqdm>=4.66.0  # 新增：data_process.py 使用进度条

# PDF 处理
PyMuPDF>=1.24.0  # 新增：ingest_clinical_guides.py 使用 PyMuPDF (fitz)
rapidocr-onnxruntime>=1.3.0  # 新增：ingest_clinical_guides.py 使用 RapidOCR

# 可选：生产环境监控
prometheus-client>=0.20.0  # 新增（可选）：性能监控